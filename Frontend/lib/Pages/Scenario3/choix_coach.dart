import 'package:flutter/material.dart';

class ChoixCoach extends StatelessWidget {
  const ChoixCoach({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Choix coach'),
      ),
    );
  }
}