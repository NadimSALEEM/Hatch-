import 'package:flutter/material.dart';

class ProgresHabitude extends StatelessWidget{
  const ProgresHabitude({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Progrès habitude'),
      ),
    );
  }
}